import { Dimensions, StyleSheet } from 'react-native';

export default StyleSheet.create({

    viewport: {flex:1},

    switch: {
        alignItems: 'flex-end',
        paddingHorizontal: 5,
    },

});